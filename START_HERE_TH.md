# เริ่มใช้งาน Growth Quant Alert บน GitHub + Telegram

เวอร์ชัน 1.0 — จัดทำ 8 กันยายน 2026

ระบบนี้คัดหุ้นจากรายชื่อที่กำหนด ให้คะแนนเพื่อศึกษาต่อ และส่งข้อความผ่าน Telegram
**ไม่มีการซื้อขาย ไม่มีการต่อโบรกเกอร์ และไม่มีการรับประกันผลตอบแทน**
คะแนน 80/100 ไม่ได้หมายถึงโอกาสขึ้น 80%

## 1. สิ่งที่ต้องเตรียม

บัญชี GitHub, แอป Telegram และ ZIP โปรเจกต์นี้ ไม่ต้องเปิดคอมพิวเตอร์ทิ้งไว้เมื่อเปิดใช้งาน GitHub Actions แล้ว
ค่าใช้งาน Actions/พื้นที่ Artifact ขึ้นกับแผนและโควตาของบัญชี อย่าเข้าใจว่ารับประกันฟรีโดยไม่มีข้อจำกัด

## 2. สร้างบอต Telegram

เปิด Telegram แล้วค้นหา **@BotFather** ซึ่งเป็นบอตทางการ ตรวจสอบชื่อให้ตรง
ส่ง `/newbot` ตั้งชื่อบอต และตั้ง username ที่ลงท้ายด้วย `bot`
BotFather จะให้ Bot Token มา เก็บไว้เป็นความลับ ห้ามส่งลงแชทสาธารณะหรือใส่ในไฟล์โค้ด

เปิดแชทกับบอตที่เพิ่งสร้าง แล้วกด **Start** หรือส่ง `/start`
ให้ใช้บอตใหม่สำหรับโปรเจกต์นี้โดยเฉพาะ ไม่ใช้บอตที่ต่อกับระบบอื่นอยู่

## 3. อัปโหลดเข้า GitHub

1. แตก ZIP ให้เรียบร้อย
2. สร้าง repository ชื่อ `growth-quant-alert` เลือก **Private**
3. อัปโหลด **ไฟล์และโฟลเดอร์ข้างใน** โฟลเดอร์ที่แตกไว้ ไม่ใช่อัปโหลด ZIP
4. ที่หน้าหลักของ repository ต้องเห็น `main.py`, `config.yaml`, `universe.yaml`, `requirements.txt`,
   `growth_alert/` และ `.github/` อยู่ระดับเดียวกัน
5. ต้องมีไฟล์ `.github/workflows/alerts.yml` ไม่ใช่ `growth-quant-alert/.github/workflows/alerts.yml`
   ซ้อนอยู่ในอีกโฟลเดอร์หนึ่ง

ผ่านเว็บไซต์ใช้ **Add file → Upload files → Commit changes**
ถ้าโฟลเดอร์ `.github` ไม่ติดไปด้วย ให้ใช้ **Add file → Create new file**
ตั้งชื่อ `.github/workflows/alerts.yml` แล้วคัดลอกเนื้อหาจากไฟล์ชื่อเดียวกันใน ZIP
ทำแบบเดียวกันกับ `telegram_setup.yml` และ `tests.yml`
อีกวิธีคือใช้ GitHub Desktop ซึ่งจะเก็บโครงสร้างโฟลเดอร์ได้ครบ

Workflow สำหรับตั้งเวลาและกดรันต้องอยู่บน default branch เช่น `main`

## 4. ใส่ Bot Token เป็น Secret

ใน repository ไปที่:

**Settings → Secrets and variables → Actions → New repository secret**

สร้างรายการนี้:

- Name: `TELEGRAM_BOT_TOKEN`
- Secret: วาง Token จาก BotFather

อย่าเติมเครื่องหมายคำพูด ไม่ต้องเติมคำว่า `bot` หน้าค่า Token
อย่าใส่ Token ใน `config.yaml`, README หรือไฟล์ Python

## 5. ให้ระบบหา Chat ID ให้

ไปที่ **Actions → Telegram Setup → Run workflow**
เลือก branch หลัก แล้วกด Run workflow

หากสำเร็จ บอตจะส่งข้อความกลับมาใน Telegram พร้อม Chat ID ของคุณ
คัดลอกเฉพาะค่าตัวเลข แล้วกลับไปสร้าง Secret อีกตัว:

- Name: `TELEGRAM_CHAT_ID`
- Secret: ค่า Chat ID ที่บอตส่งมา

Chat ID จะถูกส่งเข้าบอตส่วนตัว ไม่ได้พิมพ์ลง Actions log

ถ้าไม่สำเร็จ ให้ส่ง `/start` ไปที่บอตอีกครั้ง แล้วลอง Run workflow ใหม่
ตัวช่วยนี้ออกแบบสำหรับบอตใหม่ที่มีแชทส่วนตัวของคุณเพียงคนเดียว
หากมีหลายคนคุยกับบอตหรือบอตมี webhook อยู่ จะหยุดเพื่อไม่ส่ง Chat ID ผิดคน
ข้อความที่ Telegram ให้ `getUpdates` อ่านได้มีอายุจำกัด จึงควรส่งข้อความใหม่ก่อนตั้งค่า

## 6. ทดสอบข้อความด้วย Demo

ไปที่ **Actions → Growth Alerts → Run workflow**

- `mode`: เลือก `demo`
- `dry_run`: ไม่ต้องติ๊ก ถ้าต้องการให้ส่งเข้า Telegram

บอตจะส่งข้อความที่ขึ้นต้นว่า **DEMO - SYNTHETIC DATA - NOT LIVE**
ข้อมูลทุกตัวใน Demo เป็นข้อมูลสมมติ ใช้ทดสอบการคำนวณและการส่งข้อความเท่านั้น
Demo ไม่ได้ดึงราคาจริง และไม่เปลี่ยนประวัติการแจ้งเตือนจริง

ถ้าติ๊ก `dry_run` ระบบจะสร้างไฟล์รายงานอย่างเดียว ไม่ส่ง Telegram

## 7. ลองสแกนข้อมูลจริงทันที

Run workflow อีกรอบ:

- `mode`: เลือก `manual`
- `dry_run`: ไม่ติ๊ก

โหมดนี้สแกนรายชื่อจริงทันทีโดยไม่ต้องรอเวลาเปิดหรือปิดตลาด
อาจได้รับข้อความว่าไม่มีตัวใหม่ผ่านเกณฑ์ หรือข้อมูลไม่เพียงพอ ซึ่งเป็นผลลัพธ์ที่ระบบตั้งใจให้เกิดได้
ระบบไม่ฝืนเลือกหุ้นให้ครบ 5 ตัว

หลังจบ ไปที่หน้า workflow run แล้วดู **Artifacts**
ไฟล์รายงานประกอบด้วย `ranking.csv`, `snapshot.json`, `telegram_preview.txt` และ `installed_versions.txt`
ดูเหตุผลที่หุ้นถูกตัดออกได้ใน `exclusions` และดูข้อมูลขาด/ความเสี่ยงใน `flags`

## 8. รอบอัตโนมัติ

ไฟล์ Workflow มีตารางอยู่แล้ว ไม่ต้องกรอกเวลาทุกวัน:

| รอบ | เวลา America/New_York | การทำงาน |
|---|---|---|
| ก่อนเปิด | 08:37 | เริ่มสแกนก่อนเวลาเปิดปกติ 53 นาที |
| ตรวจวันปิดครึ่งวัน | 12:07 | ทำงานเฉพาะวันที่ปฏิทินระบุว่าปิดเร็ว |
| ก่อนปิดปกติ | 15:07 | เริ่มสแกนก่อนเวลาปิดปกติ 53 นาที |

วันที่เปิดเต็มวัน รอบ 12:07 จะข้าม ไม่ส่งข้อความ
วันที่ปิดครึ่งวัน รอบ 15:07 จะข้าม
วันหยุดตลาด/เสาร์อาทิตย์จะไม่ส่งรายชื่ออัตโนมัติ

โปรแกรมใช้ปฏิทิน XNYS และเวลา New York เพื่อจัดการ DST
หากตลาดเปลี่ยนวันหยุดเป็นกรณีพิเศษ ต้องอัปเดตแพ็กเกจปฏิทิน/ตรวจประกาศตลาดด้วย

เวลาไทยโดยประมาณของการเริ่มสแกน:
- ช่วง New York ใช้ DST: ก่อนเปิด 19:37 / ก่อนปิด 02:07 ของวันถัดไป
- ช่วงไม่มี DST: ก่อนเปิด 20:37 / ก่อนปิด 03:07 ของวันถัดไป
- เวลา Singapore เร็วกว่าเวลาไทย 1 ชั่วโมง
- วันปิดครึ่งวันจะใช้รอบเที่ยง New York แทน

นี่คือเวลา **เริ่มรัน** ไม่ใช่เวลาที่รับประกันว่าข้อความจะถึง
GitHub อาจเข้าคิวช้า และการดึงข้อมูลใช้เวลา หากหลุดช่วงที่กำหนด ระบบจะงดส่งรายชื่อก่อนตลาด
GitHub Actions ไม่เหมาะกับงานที่ต้องถึงตรงวินาทีหรือใช้เป็นระบบฉุกเฉิน

เปิด/ปิดรอบอัตโนมัติได้ที่ **Actions → Growth Alerts → เมนูจุดสามจุด → Disable workflow**
อย่ารันบอตซ้ำจาก repository หลายสำเนา เพราะแต่ละสำเนามีประวัติแยกกัน

## 9. คะแนนที่ติดตั้งมา

ระบบมี 13 ตัวแปรคะแนนใน 5 กลุ่ม:

| กลุ่ม | น้ำหนักเริ่มต้น |
|---|---:|
| Growth | 25% |
| Profitability / Cash Quality | 25% |
| Earnings Expectations | 20% |
| Momentum | 20% |
| Valuation | 10% |

น้ำหนักเป็นสมมติฐานเริ่มต้นที่ปรับจากข้อจำกัดข้อมูลฟรี ไม่ใช่น้ำหนักที่ผ่าน Backtest แล้ว
อ่านสูตรทั้งหมดได้ใน `docs/FACTOR_REGISTRY.md` และข้อจำกัดงานวิจัยใน `docs/RESEARCH.md`

เมื่อข้อมูลปัจจัยขาด จะใช้คะแนนกลาง 50 สำหรับปัจจัยนั้น ไม่เพิ่มน้ำหนักให้ปัจจัยที่เหลือ
จากนั้นหักคะแนนตาม Coverage และบล็อกหุ้นที่ข้อมูลไม่พอ
บางปัจจัยใช้กลุ่ม Sector ถ้ามีจำนวนตัวอย่างพอ มิฉะนั้นใช้ Growth universe และระบุไว้
Valuation จะไม่เทียบข้าม Sector หากจำนวนตัวอย่างไม่พอ

EPS Revision เป็น snapshot จาก Yahoo ซึ่งไม่ได้ตรวจยืนยัน fiscal-period rollover อย่างอิสระ
จึงถือเป็นปัจจัยทดลอง ไม่ใช่ฐานประมาณการย้อนหลังแบบ point-in-time
ไม่มี Revenue/EPS Surprise, ข่าว AI, guidance หรือวันประกาศงบที่ตรวจสอบแล้วในเวอร์ชันนี้

## 10. ปรับรายชื่อหุ้นและเกณฑ์

แก้ `universe.yaml`:
- `core`: หุ้นที่จะเข้าเกณฑ์คะแนน Growth
- `speculative`: หุ้นที่แยกติดตามความเคลื่อนไหวราคา ไม่มี Research Score

รายชื่อที่ให้มาเป็น seed 40 ตัว และ speculative 3 ตัว ไม่ใช่รายชื่อทั้งตลาด
ไม่ได้ยืนยันว่าทุกชื่อยังจดทะเบียนอยู่หรือผ่านนิยาม Growth ณ ปัจจุบัน
โปรแกรมจะอ่านข้อมูลและคัดกรองอีกครั้ง อาจต้องลบ/เปลี่ยน ticker เมื่อบริษัทเปลี่ยนชื่อหรือถูกซื้อกิจการ

แก้ `config.yaml` เช่น `top_n`, `min_score`, `min_revenue_yoy`,
`min_dollar_volume_20d`, `repeat_after_days` และน้ำหนัก
อย่าปรับหลายเกณฑ์จากผลช่วงสั้น ๆ เพื่อให้ผลย้อนหลังดูดี

การเปลี่ยน `display_timezone` เปลี่ยนแค่เวลาในข้อความ ไม่เปลี่ยนตาราง NY
การเปลี่ยนตารางเวลา ต้องแก้ `.github/workflows/alerts.yml` ให้สอดคล้องกับช่วงที่ config อนุญาต

## 11. ประวัติและการกันแจ้งซ้ำ

ระบบบันทึกผลการส่ง ประวัติคะแนน และแคชงบเป็น GitHub Actions Artifact ชื่อ `growth-alert-state`
รอบถัดไปจะกู้จาก Artifact ของ default branch
ไม่ต้องให้สิทธิ์เขียนโค้ดกลับเข้า repository

มีการกันส่งซ้ำต่อรอบก่อนเปิด/ก่อนปิด และลดการแจ้งหุ้นเดิมถ้าไม่มีอะไรเปลี่ยน
แต่ไม่รับประกัน exactly-once เมื่อ Telegram/เครือข่ายล้มเหลวระหว่างส่งหรือ Artifact สูญหาย
หากส่งข้อความหลายส่วนสำเร็จบางส่วนแล้วล้มเหลว รอบถัดไปอาจมีบางส่วนซ้ำ

Artifact ไม่ใช่ฐานข้อมูลถาวร มี retention และโควตาพื้นที่
ควรสำรอง `journal.jsonl` เป็นระยะ ถ้าหยุดใช้นานจน Artifact หมดอายุ ประวัติอาจเริ่มใหม่
ตัวกู้ประวัติจำกัดขนาดไฟล์แตกทั้งหมด 100 MB เพื่อความปลอดภัย

## 12. ข้อจำกัดสำคัญก่อนใช้ข้อมูลจริง

- yfinance เป็นเครื่องมือไม่เป็นทางการ ข้อมูลอาจช้า ขาด หรือถูก rate-limit
- ข้อมูล Yahoo มีข้อกำหนดการใช้งาน โปรเจกต์นี้ออกแบบสำหรับการศึกษาส่วนตัว ไม่ให้เผยแพร่ชุดข้อมูลต่อโดยอัตโนมัติ
- หากไม่มีข้อมูลก่อนเปิด/ระหว่างวันสดพอ จะแสดง **PRIOR CLOSE ONLY** ไม่แกล้งใช้ราคาวันก่อนเป็นราคาปัจจุบัน
- Volume ระหว่างวันเทียบกับเวลาเดียวกันของวันก่อน ไม่ใช่เทียบเต็มวัน
- คะแนนไม่ได้พิสูจน์ผลตอบแทนเหนือ benchmark และไม่มี Backtest ที่อ้างว่าแม่น
- Cash runway เป็น proxy จากเงินสดกับ FCF ย้อนหลัง ไม่ใช่การพยากรณ์ฐานะการเงินหรือการตรวจหนี้ครบทุกสัญญา
- ไม่มีการตรวจ bid–ask spread, บริษัทเพิ่มทุน, ข่าวสำคัญ, guidance หรือความเสี่ยงงบจะออกครบถ้วน
- ควรตรวจงบ/ประกาศบริษัทและราคาจากแหล่งที่เชื่อถือได้ก่อนตัดสินใจเอง

## 13. สถานะการทดสอบของไฟล์ที่ส่งมอบ

โค้ดหลักและสถานการณ์จำลองทดสอบในสภาพแวดล้อมออฟไลน์ รวมถึงปฏิทินจริงของแพ็กเกจ
อ่านรายการและจำนวนการทดสอบล่าสุดใน `docs/VALIDATION.md`

**ยังไม่ได้เชื่อมต่อกับ GitHub หรือ Telegram ของคุณ**
ไม่มี Token หรือ Chat ID ของคุณอยู่ในไฟล์
ยังไม่ได้ยืนยันการดึงข้อมูล Yahoo และการส่ง Telegram แบบ end-to-end จากสภาพแวดล้อมจัดทำนี้
จึงต้องทำขั้นตอน Demo แล้วตามด้วย Manual live scan บน GitHub ของคุณ

## V1.1 - แก้ปัญหา `Data usable: 0/40`

V1.1 เปลี่ยน data architecture ดังนี้:

- ราคายังคงใช้ Yahoo/yfinance สำหรับ daily/intraday market data
- ถ้างบ Yahoo ขาด ระบบจะลองใช้ SEC EDGAR Company Facts เป็น fallback สำหรับงบที่ยื่นต่อ SEC
- การขาด EPS revision, valuation หรือ fundamental field บางตัวจะลด `Coverage` และ `Confidence` ของหุ้นนั้น แทนที่จะทำให้หุ้นทั้ง universe ใช้งานไม่ได้
- safety gate ระดับทั้งระบบดูจาก **current price availability** เป็นหลัก ส่วน fundamental coverage ถูกประเมินแยกรายหุ้น
- cache รุ่นเก่าจะถูก invalidate อัตโนมัติหลังอัปเกรดเป็น V1.1

ถ้าต้องการระบุตัวตนให้ SEC ชัดเจนขึ้น ให้สร้าง GitHub Actions secret ชื่อ `SEC_USER_AGENT` เช่น
`Your Name your-email@example.com` ตามนโยบายการเข้าถึงข้อมูลของ SEC. หากไม่ตั้ง ระบบจะใช้ชื่อโปรเจกต์และ URL repository เป็นค่าเริ่มต้น

หลังอัปโหลด V1.1 ให้ไปที่ **Actions -> Growth Alerts -> Run workflow -> mode = manual** และรันใหม่ จากนั้นดูบรรทัด `Price usable`, `Fundamental usable`, `Eligible`, `Coverage` และ `Confidence` ในผลลัพธ์
